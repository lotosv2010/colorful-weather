<!--components/page-header/index.wxml-->
<view class="header-city" wx:if="{{district || city || province}}">
  <text class="city-name">{{district}}{{district && city ? ' · ' : ''}}{{city}}{{(district || city) && province ? ' · ' : ''}}{{province}}</text>
</view>
