<!--components/page-header/index.wxml-->
<view class="header-city" wx:if="{{district || city || province}}">
  <text class="city-name">{{district}}{{district && displayCity ? ' · ' : ''}}{{displayCity}}{{(district || displayCity) && province ? ' · ' : ''}}{{displayCity ? '' : province}}</text>
</view>
